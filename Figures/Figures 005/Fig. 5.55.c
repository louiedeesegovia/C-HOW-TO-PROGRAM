#include <stdio.h>
#include <math.h>

int main() {
    double x = 5.7;
    double result = floor(x);
    printf("floor(%.1f) = %.1f\n", x, result); // Output: floor(5.7) = 5.0
    return 0;
}

