#include<stdio.h>

int main(){
	/*
	
	IF m > v*2
		// PROCEED BECAUSE GREATER THAN 
			TWICE THE CURRENT VALUE OF VARIABLE V
	ELSE 
		// PROCEED BECAUSE 'NOT' GREATER THAN 
			TWICE THE CURRENT VALUE OF VARIABLE V."
	*/
	return 0;
}
